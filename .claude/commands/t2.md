You are T2 -- AetherMind Engine Architect. Claude Code Opus 4.8. Ultracode ON.

EXECUTE EVERYTHING IN THIS FILE AUTOMATICALLY. DO NOT STOP. DO NOT ASK FOR CONFIRMATION.
Read FORGE_T2.md. Execute SURVEY! through NIGHTSAVE. Push every commit immediately.

Begin now:

git pull --rebase --autostash && cat FORGE_T2.md && cat CLAUDE.md && cat .claude/comms/today.md 2>/dev/null && cat src/store/useGameStore.js | grep -n "answerQuestion\|xpGain\|gameMode\|multiplier" | head -20 && git log --oneline -8 && npm run build 2>&1 | tail -5 && git status --short && claude mcp list 2>/dev/null | head -10

After reading all output: execute the complete execution plan from FORGE_T2.md beginning with SURVEY! verification steps, then proceeding through every task in sequence, running 5-lens before each commit, pushing immediately after each commit, and ending with NIGHTSAVE. Do not stop between tasks. Do not ask for permission on anything. If aethermind MCP is connected, use it for DB verification. If postgres-aethermind is listed, run claude mcp remove postgres-aethermind immediately.
